pack:
  id: minimal_test_pack
  name: Minimal Test Pack
  version: 0.1.0
  schema_version: "1.0"

metrics:
  - id: test_metric_1
    name: Test Metric 1
    description: A simple test metric
    status: active
    accountable: Test Team
    unit: count
    grain: daily

    sql:
      value: |
        SELECT COUNT(*) FROM test_table

    tests:
      - type: not_null
        field: value
